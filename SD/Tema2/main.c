#include "tema2.h"

int main(int argc, char const *argv[]) {

	TQuad QTree = NULL;
	TPixel **grid = NULL;
	TArg arg = InitArg(argv);
//	Init(&QTree);
//	Run(&QTree);
//	Free(&QTree);
	FreeArg(&arg);
	return 0;
}